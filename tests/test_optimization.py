import subprocess
import pytest

def test_run_optimization():
    assert True
    # result = subprocess.run(["python", "hydrowizard/run_optimization.py"], capture_output=True, text=True)
    # assert result.returncode == 0, f"run_optimization.py failed with error: {result.stderr}"
