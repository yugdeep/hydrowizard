import subprocess
import pytest

def test_run_simulation():
    assert True
    # result = subprocess.run(["python", "hydrowizard/run_simulation.py"], capture_output=True, text=True)
    # assert result.returncode == 0, f"run_simulation.py failed with error: {result.stderr}"
